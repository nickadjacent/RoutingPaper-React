import React from 'react';
// import { Link } from '@reach/router';

const Number = ({ num }) => {

    return (
        <div>
            <h1>The number is: {num}</h1>
        </div>

    );
};

export default Number;